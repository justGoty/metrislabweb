# Структура сайта МэтрисЛаб

Актуальная схема локального проекта `metrislabweb`. Обновлено 9 сентября 2026 года.

## Позиционирование

Основное предложение:

> **Поверка и сервис газоанализаторов под ключ**
>
> Принимаем оборудование, проводим диагностику и ремонт, организуем поверку и возвращаем комплект документов.

МэтрисЛаб ведет заявку, прием, диагностику, согласование ремонта и выдачу. Поверку выполняет аккредитованная организация. Название партнера публично не раскрывается.

## Текущие страницы

### `/` — главная

1. Служебная строка: направление, email, телефон.
2. Навигация: услуги, процесс, лаборатория, каталог, FAQ.
3. Первый экран с реальной фотографией лаборатории.
4. Матрица компетенций.
5. Три сервисных сценария.
6. Путь прибора из пяти этапов.
7. Реальная техническая площадка в Балашихе.
8. Превью каталога.
9. Компании, с которыми есть опыт взаимодействия.
10. FAQ, включая корректное описание схемы поверки.
11. Форма заявки с вложением.
12. Контакты, реквизиты и юридическое пояснение.

### `/catalog/` — каталог

- поиск по модели, описанию, производителю и вариантам написания;
- фильтры по типу и производителю;
- адаптивная сетка 1/2/3 колонки;
- передача выбранной модели в форму;
- пустое состояние и сброс фильтров;
- данные отделены от верстки в `src/data/gasAnalyzers.ts`.

### `/privacy/` — персональные данные

- сведения об операторе и составе обрабатываемых данных;
- цели, основания, сроки и порядок обработки;
- права пользователя и контакт для обращений;
- ссылка доступна в форме заявки и подвале.

### `/404.html` — страница ошибки

Возвращает посетителя в каталог или на главную; содержит телефон. Закрыта от индексации.
На Apache подключается через `ErrorDocument 404` в `public/.htaccess`; HTTP-код после публикации проверяется отдельно.

## Компоненты

```text
src/App.tsx                            маршрутизация страниц
src/entry-server.tsx                   рендер HTML и метаданных при сборке
src/data/pageMeta.ts                   маршруты, SEO и JSON-LD
src/components/Navbar.tsx             служебная строка и навигация
src/components/Hero.tsx               первый экран
src/components/About.tsx              компетенции
src/components/Services.tsx           сервисные сценарии
src/components/Process.tsx            путь прибора
src/components/Laboratory.tsx         фотография и техническая площадка
src/components/GasAnalyzerModels.tsx  превью каталога
src/components/Clients.tsx            опыт взаимодействия
src/components/FAQ.tsx                вопросы и схема поверки
src/components/ContactForm.tsx        заявка и вложение
src/pages/CatalogPage.tsx             поиск и фильтры
src/pages/PrivacyPage.tsx             политика обработки данных
src/data/gasAnalyzers.ts              данные приборов
scripts/create-static-routes.mjs      готовые HTML-страницы из компонентов React
scripts/static-routes.test.mjs        проверки HTML, SEO и ресурсов сборки
scripts/browser-checks.js             браузерные проверки с имитацией ответа формы
public/api/contact.php                 серверная обработка формы
public/images/lab/                     фотографии лаборатории
public/images/clients/                 логотипы компаний
```

## Форма заявки

Клиент указывает имя, телефон, email и задачу, при необходимости прикладывает JPG, PNG, WEBP или PDF до 5 МБ.

Обработчик `public/api/contact.php`:

- проверяет Origin, поля, согласие, размер и MIME вложения;
- использует honeypot и ограничение частоты;
- отправляет письмо на технический адрес `gotypress3@gmail.com`;
- не содержит паролей или клиентских секретов.

Публичный email сайта: `info@metrislab.ru`.

PHP-обработчик в обновлении 9 сентября не менялся. Vite не исполняет PHP; проверка формы в браузере использует имитацию ответа сервера, без отправки писем. В `npm run preview` API возвращает поясняющую ошибку, а не исходный PHP-файл. На хостинге проверяется реальная доставка с отдельным разрешением на тест.

## Готовый HTML

`npm run build` собирает клиентский JavaScript, временный SSR-модуль в `dist-ssr` и готовые русскоязычные страницы в `dist`. На хостинг загружается только `dist`; Node.js там не требуется.

Тексты, ссылки, FAQ и модели доступны без JavaScript. React подключает интерактивность к существующей разметке. Сохраненный английский язык и модель из URL применяются после этого, чтобы не возникали несовпадения HTML. Время заполнения заявки отсчитывается при открытии страницы, а не при сборке.

`npm run preview` показывает локальную сборку и страницу 404. Это не заменяет проверку правил Apache/Nginx на REG.RU.

## Масштабирование

Ближайшие отдельные страницы:

- `/uslugi/poverka-gazoanalizatorov`;
- `/uslugi/remont-gazoanalizatorov`;
- `/catalog/<model>`;
- `/o-laboratorii`;
- `/dokumenty`;
- `/kontakty`.

Страницы моделей создаются только после подтверждения фактической возможности работ. Для каждой нужны оригинальное фото, производитель, тип, измеряемые компоненты, доступные работы, комплектность при передаче и CTA с выбранной моделью.

## SEO и достоверность

- один H1 на страницу;
- уникальные `title`, `description`, canonical и осмысленные `alt`;
- тексты отвечают на реальный запрос инженера или снабжения;
- не публикуются неподтвержденные сроки, цены, аккредитация и результаты;
- поверка не описывается как работа МэтрисЛаб от собственного имени;
- фотографии показывают реальные помещения без дорисованных документов, оборудования и статусов.

## Проверка перед публикацией

1. `npm run lint`.
2. `npm run build`.
   Затем `npm run test:static`.
3. Проверка главной и каталога на 390 и 1280+ px.
4. Поиск, фильтры, меню, FAQ и передача модели в форму.
5. Проверка телефона `+7 906 079 91 44`, email и реквизитов.
6. PHP syntax check.
7. Контрольная заявка на production и проверка входящих/спама.
8. После публикации: Яндекс Вебмастер, sitemap, canonical и индексация.

Результаты текущего этапа и оставшиеся задачи: [SEO_CONVERSION_2026-09-09.md](SEO_CONVERSION_2026-09-09.md).
